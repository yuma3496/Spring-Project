<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- Mapper & xml Mapping -->
<mapper namespace="net.myprocon.domain.user.model.MUser">

    <!-- User Registration -->
    <insert id="insertOne">
        insert into m_user(
        user_id,
        password,
        user_name,
        birthday,
        age,
        gender,
        department_id,
        role

        ) values (
        #{userId},
        #{password},
        #{userName},
        #{birthday},
        #{age},
        #{gender},
        #{departmentId},
        #{role}
        )
    </insert>

    <!-- User Registration (multiple users) -->
    <select id="findMany" resultType="MUser">
        select
        *
        from
        m_user
    </select>
</mapper>